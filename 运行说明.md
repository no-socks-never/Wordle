# 运行说明

## 🎯 推荐运行顺序

根据你的需求（三个模型的调参、静态特征工程的影响、模型效果对比），请按以下顺序运行：

### 第一步：完整超参数搜索（必需）
```bash
python param.py
```

**作用**：
- ✅ 对三个模型（LSTM、BiLSTM、Transformer）进行网格搜索
- ✅ 测试不同的超参数组合（隐藏层维度、学习率、序列长度等）
- ✅ 分别测试使用静态特征（`use_static=True`）和不使用静态特征（`use_static=False`）的效果
- ✅ 生成三个调参结果CSV文件：
  - `results/hparam_results_LSTM.csv`
  - `results/hparam_results_BiLSTM.csv`
  - `results/hparam_results_Transformer.csv`
- ✅ 为每个参数组合生成详细的训练结果（历史记录、损失曲线、预测结果等）

**运行时间**：可能需要较长时间（取决于数据量和参数组合数量）

**输出文件**：
- 3个调参结果CSV文件
- 每个参数组合的详细结果文件（约100+个文件）

---

### 第二步：模型对比分析（必需）
```bash
python compare_results.py
```

**作用**：
- ✅ 自动读取第一步生成的调参结果
- ✅ 找出每个模型的最佳配置
- ✅ **对比三个模型的最终效果**
- ✅ **分析静态特征工程的影响**（使用静态特征 vs 不使用静态特征的性能对比）
- ✅ 生成对比报告：
  - `results/model_comparison.csv` - 三个模型在各项指标上的对比
  - `results/detailed_comparison.csv` - 三个最佳模型的详细配置对比

**输出**：在终端显示详细的对比分析结果

---

## 📝 其他可选运行

### 快速测试（可选，用于验证代码是否能跑通）
```bash
python main.py
```

**作用**：
- 用默认参数快速测试三个模型
- 验证整个pipeline是否能正常运行
- **不进行调参**，只测试默认配置

**运行时间**：较短（约几分钟）

---

## 🔍 你需要的结果文件位置

运行完成后，所有结果都在 `results/` 目录下：

### 1. 三个模型的调参结果
- `results/hparam_results_LSTM.csv` - LSTM模型的调参结果（包含所有参数组合的性能）
- `results/hparam_results_BiLSTM.csv` - BiLSTM模型的调参结果
- `results/hparam_results_Transformer.csv` - Transformer模型的调参结果

### 2. 静态特征工程的影响
在每个调参结果CSV文件中，通过 `use_static` 列可以看到：
- `use_static=True` 的行：使用静态特征的性能
- `use_static=False` 的行：不使用静态特征的性能

运行 `compare_results.py` 后，会输出静态特征影响的详细分析。

### 3. 三个模型最终效果对比
运行 `compare_results.py` 后会生成：
- `results/model_comparison.csv` - 三个最佳模型在各项指标上的对比表格
- `results/detailed_comparison.csv` - 三个最佳模型的详细配置对比

终端也会输出详细的对比分析报告。

### 4. 每个模型变体的详细结果
每个参数组合会生成6个文件：
- `{model_name}_history.csv` - 训练历史
- `{model_name}_loss_curve.png` - 损失曲线图
- `{model_name}_test_preds.csv` - 测试集预测结果
- `{model_name}_test_avg_trend.png` - 平均尝试次数趋势图
- `{model_name}_attention_heatmap.png` - 注意力热力图（仅BiLSTM）
- `{model_name}_metrics.txt` - 测试集评估指标

---

## ⚠️ 注意事项

1. **运行时间**：`param.py` 需要测试很多参数组合，可能需要较长时间，请耐心等待。

2. **结果文件**：运行后会生成大量文件（每个参数组合6个文件），确保有足够的磁盘空间。

3. **查看详细说明**：更多关于结果文件的详细说明，请查看 `RESULTS_GUIDE.md`。

4. **如果只想快速验证**：可以先运行 `main.py` 确认代码能正常运行，再运行 `param.py`。


